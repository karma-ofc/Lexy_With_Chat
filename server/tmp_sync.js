const { Client } = require('pg'); const fetch = require('node-fetch'); (async ()=>{ const c = new Client({ user: 'postgres', host: 'localhost', database: 'lexy', password: 'admin', port: 5432 }); await c.connect(); const r = await c.query('SELECT u.id,u.name,u.username,u.password,u.avatar,u.notifications_enabled,r.name as role,u.created_at FROM users u LEFT JOIN roles r ON u.role_id=r.id WHERE u.id=$1',[11]); console.log('source user:', r.rows[0]); await c.end(); try{ const res = await fetch('http://localhost:3001/chat-api/internal/users/sync', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(r.rows[0]) }); console.log('sync status', res.status); console.log(await res.text()); }catch(e){ console.error('sync error', e); } })().catch(e=>{ console.error(e); process.exit(1); });